# Docker Environment Configuration
# Copy this file to .env.docker and update with your values

# Application Settings
APP_NAME=Satellite Imagery Gateway
VERSION=1.0.0
ENVIRONMENT=production
DEBUG=false

# API Configuration
API_V1_PREFIX=/api/v1

# Database (PostgreSQL in Docker)
# Using service name 'db' from docker-compose.yml
DATABASE_URL=postgresql://satellite_user:satellite_pass@db:5432/satellite_imagery

# Redis (Using service name 'redis' from docker-compose.yml)
REDIS_URL=redis://redis:6379

# JWT Configuration (CHANGE THESE IN PRODUCTION!)
SECRET_KEY=change-this-to-a-long-random-string-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# External APIs (to be implemented)
# NASA_EARTHDATA_USERNAME=your_username
# NASA_EARTHDATA_PASSWORD=your_password
# COPERNICUS_USERNAME=your_username
# COPERNICUS_PASSWORD=your_password

# Logging
LOG_LEVEL=INFO
